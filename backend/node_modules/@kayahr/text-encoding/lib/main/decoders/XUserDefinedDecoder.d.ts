import { AbstractDecoder } from "../AbstractDecoder.ts";
import { type ByteBuffer } from "../ByteBuffer.ts";
/**
 * Decoder for x-user-defined encoding.
 */
export declare class XUserDefinedDecoder extends AbstractDecoder {
    /** @inheritdoc */
    decode(buffer: ByteBuffer): number | number[] | null;
}
